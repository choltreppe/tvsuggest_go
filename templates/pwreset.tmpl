{{define "body"}}

  <form class="box" onsubmit="post(this, '/')">

    <div class="msg-box hidden"></div>

    <input type="password" name="passwd" placeholder="New Password">
    <div class="buttons">
      <div class="loading hidden"></div>
      <input type="submit" class="primary" value="Set Password">
    </div>
  </form>

{{end}}